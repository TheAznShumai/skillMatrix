<h3>Users</h3>
<table class="table table-striped table-curved" id="users">
    <thead>
      <tr>
        <th>Name</th>
        <th>Email</th>
        <th>Title</th>
        <th>Created at</th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      <% @users.each do |user| %>
          <tr>
          <td><%= link_to user.profile.full_name, profile_path(user.profile) %></td>
          <td><%= link_to user.email, '#', :class => 'mailto', "data-email" => user.email %></td>
          <td><%= user.profile.title %>
          <td><%= user.created_at %></td>
          <td>
            <%= link_to 'Edit', edit_user_path(user), :class => 'btn btn-xs btn-primary' %>
            <%= link_to 'Destroy', user_path(user), :method => :delete,
              :class => 'btn btn-xs btn-danger',
              :data => {
              :confirm => "Are you sure you want to delete #{user.email}?",
              :title => 'Delete User'} %>
          </td>
        </tr>
      <% end %>
      </tbody>
    </table>

