<h1>Survey</h1>

<% @surveys.each do |survey| %>
      <%= link_to image_tag(survey.icon(:small)), survey %>
      <%= link_to survey.name, new_or_edit_attempt_path(survey,
                               current_user.attempts.where(:survey_id => survey.id)) %>
<% end %>

